const form=document.getElementById('bookingForm');const date=document.getElementById('date');date.min=new Date().toISOString().split('T')[0];form.addEventListener('submit',e=>{e.preventDefault();const data=Object.fromEntries(new FormData(form));data.id=crypto.randomUUID?crypto.randomUUID():Date.now().toString();data.createdAt=new Date().toLocaleString();data.status='Pending';const bookings=JSON.parse(localStorage.getItem('creativeNailsBookings')||'[]');bookings.push(data);localStorage.setItem('creativeNailsBookings',JSON.stringify(bookings));document.getElementById('success').style.display='block';form.reset();window.scrollTo({top:document.body.scrollHeight,behavior:'smooth'})});
